# Usage
1. Download `IconFix.bat` to your local machine.
2. Right-click the file and select **Run as administrator**.
3. The desktop will flash briefly because Windows File Explorer restarts automatically.
4. Wait until the console shows `Done.` and press any key to close the window.